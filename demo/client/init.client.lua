--!strict
-- Client Demo Init

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Net = require(ReplicatedStorage.RoNet)

require(script.UI)
require(script.Combat)
require(script.Economy)
require(script.AdminPanel)

print("[RoNet Demo] Client initialized")
